
const data=window.MOCHI_COMMANDS||{},list=document.getElementById('commandList'),search=document.getElementById('searchInput'),menuBtn=document.getElementById('menuBtn'),navLinks=document.getElementById('navLinks'),cmdCount=document.getElementById('cmdCount');
cmdCount.textContent=Object.values(data).reduce((n,a)=>n+a.length,0);
function render(q=''){list.innerHTML='';q=q.trim().toLowerCase();Object.entries(data).forEach(([cat,items])=>{const f=items.filter(x=>x.toLowerCase().includes(q)||cat.toLowerCase().includes(q));if(!f.length)return;const w=document.createElement('article');w.className='command-group glass reveal visible';const h=document.createElement('button');h.className='command-head';h.innerHTML=`<div><strong>${cat}</strong><br><span>${f.length} lệnh</span></div><b>⌄</b>`;const b=document.createElement('div');b.className='command-body'+(q?' open':'');const inn=document.createElement('div');inn.className='command-items';f.forEach(x=>{const e=document.createElement('div');e.className='command-item';e.textContent=x;inn.appendChild(e)});b.appendChild(inn);h.onclick=()=>b.classList.toggle('open');w.append(h,b);list.appendChild(w)})}render();search.oninput=e=>render(e.target.value);menuBtn.onclick=()=>navLinks.classList.toggle('open');navLinks.querySelectorAll('a').forEach(a=>a.onclick=()=>navLinks.classList.remove('open'));const ob=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>ob.observe(e));
